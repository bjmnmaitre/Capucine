{
  "name": "capucine-core",
  "version": "0.1.0",
  "description": "Capucine \u2014 AI-powered shopping agent core",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "dev": "node --env-file-if-exists=.env --import tsx src/api/server.ts",
    "type-check": "tsc --noEmit",
    "start": "node --env-file-if-exists=.env --import tsx src/api/server.ts"
  },
  "keywords": [
    "shopping",
    "agent",
    "ai",
    "ranking",
    "offers"
  ],
  "author": "Capucine Team",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/supertest": "^7.2.1",
    "jest": "^29.7.0",
    "supertest": "^7.2.2",
    "ts-jest": "^29.1.0",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  }
}